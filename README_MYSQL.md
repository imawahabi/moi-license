# نظام رخص إدارة السجل العام - وزارة الداخلية الكويتية
## إعداد النظام للعمل مع MySQL محلياً

### 📋 المتطلبات:
- XAMPP مثبت ويعمل
- Node.js مثبت
- متصفح ويب حديث

### 🚀 خطوات التثبيت:

#### 1. إعداد قاعدة البيانات:
```bash
# 1. تشغيل XAMPP
# تأكد من تشغيل Apache و MySQL

# 2. فتح phpMyAdmin
# اذهب إلى: http://localhost/phpmyadmin

# 3. إنشاء قاعدة البيانات
# انسخ محتوى ملف database/kuwait_police_licenses.sql
# والصقه في phpMyAdmin واضغط "تنفيذ"
```

#### 2. إعداد الخادم الخلفي (Backend):
```bash
# الانتقال إلى مجلد الخادم
cd backend

# تثبيت المكتبات المطلوبة
npm install

# تشغيل الخادم
npm start
# أو للتطوير:
npm run dev
```

#### 3. إعداد الواجهة الأمامية (Frontend):
```bash
# في المجلد الرئيسي للمشروع
npm install

# بناء المشروع
npm run build

# تشغيل الخادم المحلي
npm run preview
```

### 🔧 إعدادات قاعدة البيانات:

إذا كانت إعدادات MySQL مختلفة، عدّل الملف `backend/server.js`:

```javascript
const dbConfig = {
  host: 'localhost',
  user: 'root',           // اسم المستخدم
  password: '',           // كلمة المرور (عادة فارغة في XAMPP)
  database: 'kuwait_police_licenses',
  port: 3306,
  charset: 'utf8mb4'
};
```

### 📊 هيكل قاعدة البيانات:

#### جدول الموظفين (employees):
- `id` - المعرف الفريد
- `full_name` - الاسم الكامل
- `rank` - الرتبة
- `file_number` - رقم الملف (فريد)
- `category` - الفئة (ضابط، ضابط صف، مهني، مدني)

#### جدول الرخص (licenses):
- `id` - المعرف الفريد
- `employee_id` - معرف الموظف
- `license_type` - نوع الرخصة (يوم كامل / ساعات محددة)
- `license_date` - تاريخ الرخصة
- `hours` - عدد الساعات (للرخص المحددة)
- `month` - الشهر (محسوب تلقائياً)
- `year` - السنة (محسوب تلقائياً)

### 🌐 الوصول للنظام:

بعد تشغيل الخادمين:
- الخادم الخلفي: `http://localhost:3001`
- النظام: `http://localhost:4173` (أو المنفذ المعروض)

### 📝 ملاحظات مهمة:

1. **تأكد من تشغيل XAMPP** قبل تشغيل النظام
2. **MySQL يجب أن يعمل** على المنفذ 3306
3. **قاعدة البيانات تحتوي على بيانات تجريبية** للاختبار
4. **النظام يدعم RTL** والخط العربي بالكامل

### 🔍 استكشاف الأخطاء:

#### خطأ في الاتصال بقاعدة البيانات:
- تأكد من تشغيل MySQL في XAMPP
- تحقق من اسم المستخدم وكلمة المرور
- تأكد من إنشاء قاعدة البيانات

#### خطأ في تشغيل الخادم:
- تأكد من تثبيت Node.js
- تأكد من تثبيت المكتبات: `npm install`
- تحقق من عدم استخدام المنفذ 3001

### 📞 الدعم:
في حالة وجود مشاكل، تحقق من:
1. سجلات الخادم في Terminal
2. سجلات المتصفح (F12 → Console)
3. حالة قاعدة البيانات في phpMyAdmin