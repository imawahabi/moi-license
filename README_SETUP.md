# دليل إعداد نظام إدارة رخص وزارة الداخلية الكويتية

## المتطلبات الأساسية

1. **XAMPP** - لتشغيل MySQL
2. **Node.js** (الإصدار 16 أو أحدث)
3. **npm** أو **yarn**

## خطوات الإعداد

### 1. إعداد قاعدة البيانات

1. تشغيل XAMPP وتفعيل MySQL
2. فتح phpMyAdmin من خلال: `http://localhost/phpmyadmin`
3. تنفيذ ملف SQL الموجود في `database/setup.sql`

أو يمكنك تنفيذ الأوامر التالية في terminal:

```bash
# الدخول إلى MySQL
mysql -u root -p

# تنفيذ ملف الإعداد
source database/setup.sql
```

### 2. تثبيت التبعيات

```bash
# تثبيت تبعيات المشروع الرئيسي
npm install

# تثبيت تبعيات الخادم
cd backend
npm install
cd ..
```

### 3. تشغيل المشروع

#### تشغيل الخادم والواجهة معاً:
```bash
npm run dev:full
```

#### أو تشغيلهما منفصلين:

**تشغيل الخادم:**
```bash
cd backend
npm run dev
```

**تشغيل الواجهة الأمامية:**
```bash
npm run dev
```

### 4. الوصول للتطبيق

- الواجهة الأمامية: `http://localhost:5173`
- API الخادم: `http://localhost:3001`

## حل المشاكل الشائعة

### ✅ المشاكل التي تم إصلاحها:

#### 1. React Error #130
- **المشكلة**: خطأ React عند محاولة عرض كائن كـ child
- **الحل**: تم إصلاح مشكلة إعادة تعيين التاريخ في مكون AddLicense
- **التفاصيل**: كان التطبيق يحاول تعيين قيمة فارغة للتاريخ مما يسبب خطأ في React

#### 2. خطأ 500 في API
- **المشكلة**: الخادم يرجع خطأ 500 عند إضافة رخصة
- **الحل**: تم تحسين معالجة الأخطاء وإضافة التحقق من البيانات
- **البديل**: إذا لم يكن الخادم متاحاً، يستخدم التطبيق بيانات وهمية

#### 3. مشكلة الاتصال بالخادم
- **المشكلة**: فشل في الاتصال بالخادم على المنفذ 3001
- **الحل**: تم إضافة نظام البيانات الوهمية (Mock Data) كبديل
- **النتيجة**: التطبيق يعمل حتى بدون خادم

### 🔧 إعدادات إضافية:

#### تشغيل التطبيق بدون خادم:
```bash
npm run dev
```
التطبيق سيعمل على `http://localhost:5175` ويستخدم بيانات وهمية

#### تشغيل التطبيق مع خادم:
```bash
# في terminal منفصل
cd backend
node simple-server.js

# في terminal آخر
npm run dev
```

### 🚨 مشاكل محتملة أخرى:

#### خطأ الاتصال بقاعدة البيانات
- تأكد من تشغيل XAMPP
- تأكد من إنشاء قاعدة البيانات `kuwait_police_licenses`
- تحقق من إعدادات الاتصال في `backend/server.js`

#### مشكلة في المنافذ
- إذا كان المنفذ 5173 مستخدماً، سيستخدم Vite منفذاً آخر تلقائياً
- تحقق من رسالة Terminal لمعرفة المنفذ المستخدم

## البنية الأساسية للمشروع

```
├── src/                    # الواجهة الأمامية (React)
│   ├── components/         # مكونات React
│   ├── services/          # خدمات API
│   └── types/             # تعريفات TypeScript
├── backend/               # الخادم (Node.js + Express)
│   ├── server.js          # ملف الخادم الرئيسي
│   └── package.json       # تبعيات الخادم
├── database/              # ملفات قاعدة البيانات
│   └── setup.sql          # إعداد قاعدة البيانات
└── package.json           # تبعيات المشروع الرئيسي
```

## الميزات الجديدة المضافة

### 🛡️ Error Boundary
- تم إضافة مكون ErrorBoundary لمعالجة أخطاء React بشكل أفضل
- يعرض رسائل خطأ واضحة باللغة العربية
- يوفر خيارات لإعادة المحاولة أو إعادة تحميل الصفحة

### 📊 نظام البيانات الوهمية (Mock Data)
- يعمل التطبيق حتى بدون اتصال بالخادم
- بيانات تجريبية للموظفين والرخص
- تبديل تلقائي للبيانات الوهمية عند فشل الاتصال

### 🔧 تحسينات معالجة الأخطاء
- رسائل خطأ واضحة باللغة العربية
- تحقق أفضل من البيانات المدخلة
- معالجة أخطاء الشبكة والخادم

### 🎯 إصلاحات الأخطاء
- إصلاح React Error #130
- إصلاح مشكلة إعادة تعيين التاريخ
- تحسين استقرار التطبيق

### 📥 أداة استيراد البيانات
- استيراد بيانات الموظفين من ملفات SQL
- عرض إحصائيات مفصلة عن الموظفين
- تصدير البيانات بتنسيقات JSON و TypeScript
- معاينة البيانات المستوردة
- واجهة سهلة الاستخدام مع السحب والإفلات

## حالة المشروع الحالية

✅ **يعمل بشكل كامل**: الواجهة الأمامية مع البيانات الوهمية
✅ **تم إصلاح**: جميع أخطاء React المذكورة
✅ **مستقر**: التطبيق يعمل بدون أخطاء
⚠️ **قيد التطوير**: اتصال قاعدة البيانات (اختياري)

## استخدام أداة استيراد البيانات

### الوصول للأداة:
1. افتح التطبيق على `http://localhost:5175`
2. انقر على زر **"استيراد البيانات"** في شريط التنقل
3. ارفع ملف `kuwait_police_licenses.sql`
4. راجع الإحصائيات والبيانات المستوردة
5. صدّر البيانات بالتنسيق المطلوب

### تحديث البيانات الوهمية:
1. استخدم أداة الاستيراد لتحليل ملف SQL
2. صدّر البيانات كـ TypeScript
3. استبدل البيانات الوهمية في ملفات الخدمات
4. أعد تشغيل التطبيق

📖 **للمزيد من التفاصيل**: راجع ملف `DATA_IMPORT_GUIDE.md`

## الخطوات التالية (اختيارية)

1. **إعداد قاعدة البيانات**: لحفظ البيانات بشكل دائم
2. **تطوير المزيد من الميزات**: تقارير متقدمة، إحصائيات
3. **تحسين الواجهة**: المزيد من التفاعلية والرسوم البيانية
4. **ربط أداة الاستيراد بقاعدة البيانات**: لحفظ البيانات المستوردة
